spring:
  application:
    name: GitBase-Backend
  profiles:
    active: dev
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
# setting flyway config first
#flyway:
#  enabled: true
#  locations: classpath:/db/migration
#  baseline-on-migrate: true
#  validate-on-migrate: true
#  clean-disabled: true
